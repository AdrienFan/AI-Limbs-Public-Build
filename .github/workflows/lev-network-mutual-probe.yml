name: LEV Network Mutual Probe Build

on:
  push:
    branches:
      - experiment/lev-network-mutual-probe
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - uses: android-actions/setup-android@v4
      - name: Install Android SDK
        run: sdkmanager "platform-tools" "platforms;android-36" "build-tools;35.0.0"
      - name: Build both network probes
        run: |
          chmod +x gradlew
          ./gradlew -p experiments/lev-network-mutual-probe :guardian:assembleDebug :target:assembleDebug --no-daemon --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: lev-network-mutual-probe-apks
          path: |
            experiments/lev-network-mutual-probe/guardian/build/outputs/apk/debug/guardian-debug.apk
            experiments/lev-network-mutual-probe/target/build/outputs/apk/debug/target-debug.apk
          if-no-files-found: error
