# 静态核验与云端构建 [READY FOR CI]

- 检查 V0.5.5 包名与 V0.5.4 隔离。
- 检查工具注册、Dispatcher 路由与权限链。
- 检查接入管理不再引用工具手册编辑器或说明字符串。
- 检查 UI 后端不存在注册时缓存。
- 不在本地下载 Gradle 依赖；推送后由 GitHub Actions `android-build.yml` 编译。

## 静态记录

- `git diff --check` 通过。
- 中英文资源 XML 解析通过，新增资源无重复名称。
- UI 工具注册中不存在启动时缓存的 `uiTools` 实例；九个 UI 执行入口均动态解析后端。
- Ubuntu 空闲策略、UI 状态路由、隐藏工具手册界面和 V0.5.5 独立包名不变量均已检查。
- 按约定未运行本地 Gradle，也未下载本地构建依赖。
