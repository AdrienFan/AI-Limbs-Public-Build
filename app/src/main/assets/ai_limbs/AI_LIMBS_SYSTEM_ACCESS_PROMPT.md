# Deprecated runtime asset

AI Limbs 0.6.4.7.4 no longer loads policy from this asset.
The immutable access bootstrap and its version are generated by
`AiLimbsSystemAccessPrompt` at `code://ai_limbs/immutable_access_bootstrap`.

This tombstone remains only so upgrades and source audits can identify the removed asset path.
